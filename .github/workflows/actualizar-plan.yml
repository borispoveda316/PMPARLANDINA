name: Actualizar datos públicos del plan

on:
  repository_dispatch:
    types: [actualizar-plan-pmparlandina]

permissions:
  contents: write

concurrency:
  group: actualizar-plan-${{ github.event.client_payload.sync_id }}-${{ github.event.client_payload.index }}
  cancel-in-progress: false

jobs:
  actualizar:
    runs-on: ubuntu-latest
    steps:
      - name: Obtener repositorio
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Recibir datos del Excel
        env:
          SYNC_ID: ${{ github.event.client_payload.sync_id }}
          CHUNK_INDEX: ${{ github.event.client_payload.index }}
          CHUNK_TOTAL: ${{ github.event.client_payload.total }}
          CHUNK_DATA: ${{ github.event.client_payload.chunk }}
        run: node scripts/receive-sync.mjs

      - name: Publicar cambios
        env:
          SYNC_ID: ${{ github.event.client_payload.sync_id }}
          CHUNK_INDEX: ${{ github.event.client_payload.index }}
          CHUNK_TOTAL: ${{ github.event.client_payload.total }}
          CHUNK_DATA: ${{ github.event.client_payload.chunk }}
        run: |
          git config user.name "Acreditación Andina Sync"
          git config user.email "actions@users.noreply.github.com"
          for attempt in 1 2 3 4 5 6 7 8; do
            git add -A
            if git diff --cached --quiet; then
              exit 0
            fi
            git commit -m "Actualizar datos públicos del plan"
            if git push origin HEAD:main; then
              exit 0
            fi
            sleep $((attempt * 2))
            git fetch origin main
            git reset --hard origin/main
            node scripts/receive-sync.mjs
          done
          exit 1
